<UserControl x:Class="ExcelAutomation.Views.DataProcessingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xml:lang="ja-JP"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="データ取得" Style="{StaticResource MaterialDesignHeadline4TextBlock}" Margin="0,0,0,20" />

        <materialDesign:Card Grid.Row="1" Padding="20" materialDesign:ElevationAssist.Elevation="Dp2" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="処理対象の選択" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <DatePicker Grid.Column="0" 
                                materialDesign:HintAssist.Hint="検索対象月を選択" 
                                materialDesign:TextFieldAssist.HasClearButton="True"
                                Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                                SelectedDate="{Binding TargetDate}"
                                xml:lang="ja-JP"
                                VerticalAlignment="Bottom"
                                Margin="0,0,20,0"
                                CalendarOpened="DatePicker_CalendarOpened" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom">

                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Background="Green" BorderBrush="Green"
                                Margin="0,0,0,10"
                                Width="140"
                                Command="{Binding ExportCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileExcel" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBlock Text="月次集計出力"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource MaterialDesignRaisedDarkButton}"
                                Width="140"
                                Command="{Binding SearchCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Magnify" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBlock Text="検索"/>
                            </StackPanel>
                        </Button>

                    </StackPanel>
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <DockPanel Grid.Row="2" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                <materialDesign:PackIcon Kind="TableSearch" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Text="日別売上 検索結果" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" FontWeight="Bold"/>
            </StackPanel>
        </DockPanel>

        <DataGrid Grid.Row="3" 
                  ItemsSource="{Binding DailySalesList}" 
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionUnit="FullRow"
                  HeadersVisibility="All"
                  materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="日付" Binding="{Binding SalesDate, StringFormat=yyyy/MM/dd}" Width="120"/>
                <DataGridTextColumn Header="商品コード" Binding="{Binding ProductCode}" Width="120"/>
                <DataGridTextColumn Header="数量" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="単価" Binding="{Binding UnitPrice, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="合計金額" Binding="{Binding TotalPrice, StringFormat=C}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <materialDesign:Snackbar MessageQueue="{Binding MessageQueue}"
                                 Grid.RowSpan="4"
                                 VerticalAlignment="Bottom" />

    </Grid>
</UserControl>